# This is a basic workflow to help you get started with Actions.

name: Node.js CI

# Controls when the workflow will run
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build-and-test:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout repository
        uses: actions/checkout@v3

      # Sets up Node.js
      - name: Use Node.js 18.x
        uses: actions/setup-node@v3
        with:
          node-version: 18.x
          cache: 'npm'

      # Installs dependencies
      - name: Install dependencies
        run: npm install

      # Runs tests
      - name: Run tests
        run: npm test

      # ------------------------------------------------------------------
      # Optional: Docker Build and Push (uncomment and configure secrets)
      # This part builds a Docker image and pushes it to a registry like Docker Hub.
      # You need to set up DOCKER_USERNAME and DOCKER_PASSWORD as secrets in your GitHub repository settings.
      # ------------------------------------------------------------------

      # - name: Set up QEMU
      #   uses: docker/setup-qemu-action@v2

      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v2

      # - name: Login to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}

      # - name: Build and push
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: .
      #     push: true # Set to true to push the image
      #     tags: ${{ secrets.DOCKER_USERNAME }}/stock-management-app:latest # Replace with your desired image name
